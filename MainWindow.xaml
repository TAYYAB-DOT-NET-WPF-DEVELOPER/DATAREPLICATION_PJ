<Window x:Class="DataIntegration.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataIntegration"
        mc:Ignorable="d"
        Title="Pixel DataIntegration"
        Height="500" 
        Width="800"
        Background="Transparent"
        WindowStartupLocation="CenterScreen" 
>
    <Window.Resources>
        <LinearGradientBrush x:Key="HeaderBackgroundBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#FF1E1E30" Offset="0"/>
            <GradientStop Color="#FF45455E" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="BodyBackgroundBrush" Color="White" />
    </Window.Resources>
    <Border CornerRadius="5" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>

                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="Transparent">
                <ListView Grid.Row="1"
                         BorderBrush="Brown"
                         BorderThickness="2"
                         ItemsSource="{Binding Alllogs}" 
                         MaxHeight="{Binding ActualHeight, ElementName=listview}"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.CanContentScroll="True"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled" FontSize="25" >
                    <ListView.View>
                        <GridView >
                            <GridView.Columns>
                                <GridViewColumn Header="Description" Width="450">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Descript}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Status" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Status}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="%" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding ETA}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView.Columns>
                            <GridView.ColumnHeaderContainerStyle>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="IsEnabled" Value="False"/>
                                    <Setter Property="Background" Value="Brown"/>
                                    <Setter Property="FontSize" Value="25"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Foreground" Value="{DynamicResource Color0}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </GridView.ColumnHeaderContainerStyle>
                        </GridView>
                    </ListView.View>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Border>
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border CornerRadius="5" Grid.Row="3" Grid.ColumnSpan="4" BorderBrush="Brown" BorderThickness="1">
                </Border>
                <Border CornerRadius="5" Margin="5" Grid.Row="3" Grid.Column="0" Background="Brown">
                    <TextBlock Text= "{Binding Storename}" FontWeight="SemiBold" Foreground="{DynamicResource Color0}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border CornerRadius="5" Margin="5" Grid.Row="3" Grid.Column="1" Background="Brown">
                    <TextBlock Text= "{Binding BusinessDate}" FontWeight="SemiBold" Foreground="{DynamicResource Color0}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border CornerRadius="5" Margin="5" Grid.Row="3" Grid.Column="2" Background="Brown">
                    <TextBlock Text= "{Binding CurrentTime}" FontWeight="SemiBold" Foreground="{DynamicResource Color0}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
